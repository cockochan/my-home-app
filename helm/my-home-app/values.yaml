frontend:
  replicaCount: 1
  image:
    repository: ghcr.io/cockochan/my-home-app-frontend
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 80

backend:
  replicaCount: 1
  image:
    repository: ghcr.io/cockochan/my-home-app-backend
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 5000

postgres:
  enabled: true
  storage: 2Gi
  image: postgres:16-alpine
  username: appuser
  password: apppassword
  database: myhomeapp

ingress:
  enabled: false
  className: ""
  host: my-home-app.local